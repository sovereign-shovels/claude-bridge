{
  "name": "claude-bridge",
  "version": "0.1.0",
  "description": "Shared context bus. What you copy in Cursor is what Claude Desktop sees.",
  "type": "module",
  "bin": {
    "claude-bridge": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "prepare": "npm run build"
  },
  "dependencies": {
    "express": "^4"
  },
  "devDependencies": {
    "typescript": "^5.4",
    "@types/express": "^4",
    "@types/node": "^20"
  }
}
